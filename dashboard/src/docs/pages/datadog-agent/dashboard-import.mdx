---
title: Dashboard Import
description: Import your existing Datadog dashboard exports into Moneat with automatic widget translation.
---

# Dashboard Import

## Overview

If you're evaluating Moneat as a Datadog alternative, start with one dashboard export. The importer
translates the exported JSON into Moneat's native format, including widget types, metric queries, and
template variables.

Use the public [Datadog dashboard import page](/datadog-dashboard-import) when you want a low-friction
conversion path before changing production telemetry.

## Importing a Dashboard

<StepList
  steps={[
    {
      title: 'Export from Datadog',
      content: 'In your Datadog dashboard, click the gear icon → Export Dashboard JSON. Copy the JSON content.',
    },
    {
      title: 'Open the import dialog',
      content: 'In Moneat, go to Dashboards and click Import. Paste the exported JSON into the editor.',
    },
    {
      title: 'Review and save',
      content:
        'Moneat translates widgets, metrics, and template variables automatically. Review supported widgets, ' +
        'warnings, and telemetry requirements, then save the imported dashboard.',
    },
  ]}
/>

:::info
Dashboard import moves the saved view. Live data still requires pointing the Datadog Agent or
OTLP exporters at Moneat.
:::

## Supported Widget Types

The importer maps Datadog widget types to their Moneat equivalents:

| Datadog Widget | Moneat Widget |
|---|---|
| Timeseries | Timeseries |
| Top List | Top List |
| Query Value | Stat |
| Table | Table |
| Heatmap | Heatmap |
| Distribution | Bar |
| Pie / Sunburst | Donut |
| Note / Free Text | Text |

## Metric Translation

Datadog metric namespaces are automatically mapped to Moneat's metric system:

- **System metrics** - `system.cpu.*`, `system.mem.*`, `system.disk.*`, `system.net.*`
- **Trace metrics** - `trace.*` APM metrics
- **Log metrics** - `logs.*` log-based metrics
- **Container metrics** - `container.*`, `docker.*` metrics
- **Network metrics** - `network.*` flow metrics

## Template Variables

Datadog template variables (like `$env`, `$service`, `$host`) are preserved during import and mapped to Moneat's variable system. You can edit them after import in the dashboard settings.

:::info[Some Datadog-specific widgets or custom metric queries may not have a direct equivalent. The importer will flag these with warnings so you can adjust them manually after import.]
:::
