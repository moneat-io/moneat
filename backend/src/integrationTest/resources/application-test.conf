// Test configuration for integration tests
// Overrides main application.conf settings

testing {
    backgroundJobsEnabled = false
    externalIntegrationsEnabled = false
}

billing {
    stripeEnabled = false
    enforcementEnabled = false
    backgroundJobsEnabled = false
}

retention {
    backgroundJobsEnabled = false
}

email {
    provider = "mock"
}
